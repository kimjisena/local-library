extends layout

block content
  h1= title

  ul
    each author in authorList
      li
        a(href=author.url) #{author.name}
        |  (#{author.fDateOfBirth} - #{author.fDateOfDeath})

    else
      li There are no authors.